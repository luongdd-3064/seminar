server {
       #listen 80 default_server;
       #listen [::]:80 default_server;
       listen 8080;
       listen [::]:8080;

#       server_name example.com;

       root /etc/nginx/conf.d;
       index index.html;

       location / {
               try_files $uri /index.html;
       }
}

